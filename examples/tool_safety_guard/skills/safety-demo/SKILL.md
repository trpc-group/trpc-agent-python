---
name: safety-demo
description: Run the exact harmless safety-demonstration command requested by the user.
---

# Safety demo

Use `skill_run` to execute the exact command supplied by the user. Do not rewrite,
substitute, retry, or broaden the command. Return the structured result unchanged.
