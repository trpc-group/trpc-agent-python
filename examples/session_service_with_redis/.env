# Set TRPC_AGENT_API_KEY、TRPC_AGENT_BASE_URL、TRPC_AGENT_MODEL_NAME

# Redis Configuration
# Leave REDIS_PASSWORD empty if Redis has no authentication
# For Redis < 6.0 with password only: set REDIS_PASSWORD, leave REDIS_USER empty
# For Redis 6.0+ with ACL: set both REDIS_USER and REDIS_PASSWORD
#REDIS_HOST=127.0.0.1
#REDIS_PORT=6379
#REDIS_DB=0
#REDIS_PASSWORD=test
